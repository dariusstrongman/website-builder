const fs=require('fs');const root=fs.existsSync('dist/index.html')?'dist':'.';let bad=[];for(const f of fs.readdirSync(root).filter(f=>f.endsWith('.html'))){const s=fs.readFileSync(root+'/'+f,'utf8');if(/\bDarius\s+Stroman\b/i.test(s))bad.push(f)}if(bad.length)throw Error('Personal identity in public pages: '+bad.join(', '));console.log('Privacy check passed: no personal-name bylines in public HTML.');
