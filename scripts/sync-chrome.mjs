import {readFileSync,writeFileSync,readdirSync} from 'node:fs';
import {fileURLToPath} from 'node:url';
const root=fileURLToPath(new URL('../',import.meta.url));const home=readFileSync(root+'index.html','utf8');const header=home.match(/<header class="site-header">[\s\S]*?<\/header>/)?.[0],footer=home.match(/<footer class="site-footer">[\s\S]*?<\/footer>/)?.[0];if(!header||!footer)throw Error('Canonical chrome missing from homepage');for(const file of readdirSync(root).filter(x=>x.endsWith('.html')&&x!=='index.html')){let s=readFileSync(root+file,'utf8');if(!s.includes('class="premium-site'))continue;s=s.replace(/<header class="site-header">[\s\S]*?<\/header>/,header).replace(/<footer class="site-footer">[\s\S]*?<\/footer>/,footer);writeFileSync(root+file,s)}console.log('Shared chrome synchronized from homepage.');
